{
  "name": "dovetail",
  "version": "1.0.0",
  "type": "module",
  "bin": {
    "dt": "dt.ts"
  },
  "scripts": {
    "typecheck": "bunx tsc --noEmit",
    "compile": "bun build --compile dt.ts --outfile ~/.bun/bin/dt && mkdir -p $(brew --prefix)/share/man/man1 && cp man/dt.1 $(brew --prefix)/share/man/man1/"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  }
}
